id: "@android-compose/pattern-responsive-via-window-size-class"
kind: pattern
version: "0.1.0"
description: "Read the current window dimensions through `currentWindowAdaptiveInfo()` and switch layout topology by `WindowWidthSizeClass` (Compact / Medium / Expanded) and `WindowHeightSizeClass`. Compact = phone portrait (1-pane). Medium = unfolded foldable / small tablet (1.5-pane or list-detail). Expanded = tablet / desktop / large foldable (2-pane or 3-pane). Don't write `if (configuration.screenWidthDp > 600)`-style breakpoints — that hard-codes thresholds the platform may revise and ignores the proper API."
domain: android-compose
content_hash: "sha256:5e85c9132ac04a67846a1c8c0ab568be7fa0c504882f16aa7ba13a7b34574879"
tokens:
  summary: 145
  core: 433
  full: 721
projection:
  summary: "chunks/summary.md"
  core: "chunks/core.md"
  full: "chunks/full.md"
quality:
  overall: 4
relations:
  - { type: related, target: "@android-compose/pattern-edge-to-edge-insets" }
  - { type: related, target: "@android-compose/pattern-predictive-back" }
  - { type: related, target: "@android-compose/principle-state-driven-ui" }
created_at: "2026-05-10"
